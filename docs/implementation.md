# Implementation notes

Follow `../tasks.md` for the authoritative task list.

## Bootstrapping steps

- Next.js app scaffolded with app router.
- Base Web + Styletron planned for UI layer.
- Testing via Vitest (unit) and Playwright (e2e).
- Husky + lint-staged wired to enforce lint/format/typecheck.

Add specifics here as features/config evolve.
